(() => {
  addEventListener("message", (event) => {
    const data = event.data;
    if (event.source === window && typeof data === "string" && data.endsWith("_as_req")) {
      postMessage(data.slice(0, -7) + "_as_res", "*");
    }
  });
})();
